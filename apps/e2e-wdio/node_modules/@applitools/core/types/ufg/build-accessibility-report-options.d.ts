import { type Account } from '@applitools/core-base';
/**
 * Builds the `accessibilityReportOptions` render option: forwards `runOptions` verbatim and fills the
 * `uploadUrl` template's `__report_id__` with a fresh random id. Returns `undefined` when the account
 * has no accessibility options.
 */
export declare function buildAccessibilityReportOptions({ accessibilityOptions, reportId, }: {
    accessibilityOptions?: Account['accessibilityOptions'];
    reportId?: string;
}): {
    axeCoreOptions: unknown;
    uploadUrl: string;
} | undefined;
