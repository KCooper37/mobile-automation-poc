import type { OpenSettings, Environment } from '../automation/types';
import { type FigmaMode } from './utils';
import type { FigmaNodeMetadata, FigmaViewport, FigmaBaselineLink } from './types';
export declare const DEFAULT_MAX_VIEWPORT_HEIGHT = 800;
export declare const PLACEHOLDER_TAG = "(no design)";
export declare function deduplicateBrowsers(environments: Environment[], targetWidth: number, targetHeight: number): Environment[];
export declare function buildTargetTestConfig(originalConfig: Partial<OpenSettings>, metadata: FigmaNodeMetadata, figmaViewport: FigmaViewport, baselineLink: FigmaBaselineLink | null, mode: FigmaMode): Partial<OpenSettings>;
