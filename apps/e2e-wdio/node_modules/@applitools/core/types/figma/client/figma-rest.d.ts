import { type Logger } from '@applitools/logger';
import type { Proxy, Req } from '@applitools/req';
import type { FigmaImageResult, FigmaNodeMetadata } from '../types';
export type FigmaRestOptions = {
    tempPath?: string;
    nodesCacheTtlSeconds?: number;
    maxRateLimitWaitSeconds?: number;
    /** Serve from the on-disk caches only; throw on any miss. */
    skipApi?: boolean;
    proxy?: Proxy;
    /** Defaults to `makeFigmaReq(…)`; tests inject a stub. */
    req?: Req;
    logger?: Logger;
};
/**
 * Cache names are hashed because Figma node ids are not filename-safe — an instance node id looks like
 * `I1:2;3:4`, and `:` / `;` are illegal on Windows.
 *
 * `test/fixtures/figma-cache/seed.mjs` mirrors both functions — keep them in sync.
 */
export declare function figmaNodesCacheFileName(fileId: string, uniqueNodeIds: string[]): string;
export declare function figmaImageCacheFileName(fileId: string, nodeId: string, lastModifiedUnix: number): string;
export declare function fetchFigmaMetadata(urls: string[], accessToken: string, opts?: FigmaRestOptions): Promise<FigmaNodeMetadata[]>;
export declare function fetchFigmaImages(urls: string[], accessToken: string, opts?: FigmaRestOptions): Promise<FigmaImageResult[]>;
