export declare function createPlaceholderPng(width: number, height: number, tempPath: string): Promise<string>;
export declare function createPaddedDesignImage(imagePath: string, width: number, height: number, minSize: number, tempPath: string): Promise<{
    imagePath: string;
    width: number;
    height: number;
}>;
