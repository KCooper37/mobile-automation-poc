import type { FigmaNodeRef, FigmaNodeMetadata, FigmaViewport } from './types';
export declare function isFigmaUrl(url: string): boolean;
export declare function parseFigmaNodeUrl(url: string): FigmaNodeRef;
/**
 * The tag becomes the Eyes baseline step name, and Eyes matches steps by name, so it must derive only from
 * stable identity. Figma's "Copy link to selection" appends a `t=` param that differs on every copy —
 * keeping it means re-copying the link renames the step and orphans its accepted baseline.
 *
 * Drop only `t`: `node-id` is what distinguishes two frames of the same file, so stripping the whole query
 * would give every frame in a file the same step name.
 */
export declare function figmaStepTag(figmaUrl: string): string;
export declare function resolveFigmaUrls(figmaBaselines: string | Record<string, string | string[]>, testName: string | undefined): string[] | null;
export declare const FIGMA_MODES: readonly ["figma-baseline", "test-baseline", "auto-baseline", "disabled"];
export type FigmaMode = (typeof FIGMA_MODES)[number];
export declare function resolveFigmaMode(value: string | undefined): FigmaMode;
export declare function figmaMetadataToProperties(metadata: FigmaNodeMetadata, mode: FigmaMode): Array<{
    name: string;
    value: string;
}>;
export declare function boundingViewport(viewports: FigmaViewport[]): FigmaViewport;
export declare function computeFigmaViewport(stepViewports: FigmaViewport[], maxViewportHeight: number): FigmaViewport;
export declare function paddedDesignSize(width: number, height: number, minSize: number): {
    width: number;
    height: number;
};
