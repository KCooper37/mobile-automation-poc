import { type Logger } from '@applitools/logger';
import type { Proxy, Req } from '@applitools/req';
import type { OpenSettings } from '../automation/types';
import { type FigmaMode } from './utils';
import type { FigmaViewportOptions, FigmaBaselineDeps } from './types';
export declare function setFigmaBaselineFlow(figmaUrls: string[], originalConfig: Partial<OpenSettings>, deps: FigmaBaselineDeps, mode?: Exclude<FigmaMode, 'disabled'>, viewportOpts?: FigmaViewportOptions, logger?: Logger): Promise<Partial<OpenSettings>>;
export declare function setFigmaBaseline(configuration: Partial<OpenSettings>, figmaBaselines: string | Record<string, string | string[]>, opts?: {
    accessToken?: string;
    maxRateLimitWaitSeconds?: number;
    nodesCacheTtlSeconds?: number;
    tempPath?: string;
    mode?: FigmaMode;
    maxViewportHeight?: number;
    minViewportSize?: number | null;
    /** Serve from the disk caches only; throw on any miss. */
    skipApi?: boolean;
    proxy?: Proxy;
    /** Defaults to the client's own req; tests inject a stub. */
    req?: Req;
    logger?: Logger;
}): Promise<void>;
