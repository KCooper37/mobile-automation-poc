"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setFigmaBaseline = exports.setFigmaBaselineFlow = void 0;
// Figma baseline resolver (AD-14881): orchestration only, operating directly on core's OpenSettings
// (the eyes/cypress/non-JS bindings adapt the flat config to this shape). Helpers split into ./utils,
// ./client, ./figma-images, ./target-config.
const os = __importStar(require("os"));
const logger_1 = require("@applitools/logger");
const utils_1 = require("./utils");
const target_config_1 = require("./target-config");
const client_1 = require("./client");
const figma_images_1 = require("./figma-images");
const run_figma_check_1 = require("./run-figma-check");
async function setFigmaBaselineFlow(figmaUrls, originalConfig, deps, mode = 'auto-baseline', viewportOpts = {}, logger = (0, logger_1.makeLogger)()) {
    var _a, _b;
    const maxViewportHeight = (_a = viewportOpts.maxViewportHeight) !== null && _a !== void 0 ? _a : target_config_1.DEFAULT_MAX_VIEWPORT_HEIGHT;
    const minViewportSize = (_b = viewportOpts.minViewportSize) !== null && _b !== void 0 ? _b : null;
    for (const url of figmaUrls) {
        if (url !== '' && !(0, utils_1.isFigmaUrl)(url)) {
            throw new Error(`setFigmaBaseline: "${url}" is not a valid Figma URL. ` + `To skip a test, use an empty string.`);
        }
    }
    // '' entries are placeholder steps; resolveFigmaUrls guarantees at least one real URL here.
    const realUrls = figmaUrls.filter(url => url !== '');
    if (mode === 'test-baseline') {
        logger.log(`Fetching Figma design metadata: ${realUrls[0]}${realUrls.length > 1 ? ` (+${realUrls.length - 1} more)` : ''}`);
        const rawMetadataList = await deps.fetchFigmaMetadata(realUrls);
        logger.log(`Figma node: "${rawMetadataList[0].name}" (${rawMetadataList[0].type}) ${rawMetadataList[0].width}x${rawMetadataList[0].height}, last modified ${rawMetadataList[0].lastModifiedAt}`);
        logger.log(`Mode is test-baseline — skipping Figma change detection; adjusting viewport only.`);
        // test-baseline never fetches images, so minViewportSize floors the metadata dimensions directly.
        const metadataList = minViewportSize != null
            ? rawMetadataList.map(m => ({ ...m, ...(0, utils_1.paddedDesignSize)(m.width, m.height, minViewportSize) }))
            : rawMetadataList;
        const stepViewports = metadataList.map(m => ({ width: m.width, height: m.height }));
        const figmaViewport = (0, utils_1.computeFigmaViewport)(stepViewports, maxViewportHeight);
        logger.log(`Figma viewport: ${figmaViewport.width}x${figmaViewport.height}`);
        return (0, target_config_1.buildTargetTestConfig)(originalConfig, metadataList[0], figmaViewport, null, mode);
    }
    logger.log(`Fetching Figma design: ${realUrls[0]}${realUrls.length > 1 ? ` (+${realUrls.length - 1} more)` : ''}`);
    const rawResults = await deps.fetchFigmaImages(realUrls);
    logger.log(`Figma node: "${rawResults[0].metadata.name}" (${rawResults[0].metadata.type}) ${rawResults[0].metadata.width}x${rawResults[0].metadata.height}, last modified ${rawResults[0].metadata.lastModifiedAt}`);
    const realResults = minViewportSize != null
        ? await Promise.all(rawResults.map(async (r) => {
            const padded = await deps.padDesignImage(r.imagePath, r.metadata.width, r.metadata.height, minViewportSize);
            return { imagePath: padded.imagePath, metadata: { ...r.metadata, width: padded.width, height: padded.height } };
        }))
        : rawResults;
    // The figma viewport bounds every step, so a multi-step test with differently-sized frames renders at one consistent size.
    const stepViewports = realResults.map(r => ({ width: r.metadata.width, height: r.metadata.height }));
    const figmaViewport = (0, utils_1.computeFigmaViewport)(stepViewports, maxViewportHeight);
    const figmaEnvName = `Figma ${figmaViewport.width}x${figmaViewport.height}`;
    logger.log(`Figma viewport: ${figmaViewport.width}x${figmaViewport.height}`);
    // Splice placeholder steps back at their original positions, using a blank image at the shared figma viewport.
    const results = [];
    let realResultIndex = 0;
    for (const url of figmaUrls) {
        if (url === '') {
            const imagePath = await deps.createPlaceholderImage(figmaViewport.width, figmaViewport.height);
            results.push({
                imagePath,
                metadata: {
                    name: target_config_1.PLACEHOLDER_TAG,
                    type: 'PLACEHOLDER',
                    width: figmaViewport.width,
                    height: figmaViewport.height,
                    revision: 'placeholder',
                    lastModifiedAt: '',
                },
            });
        }
        else {
            results.push(realResults[realResultIndex]);
            realResultIndex++;
        }
    }
    logger.log(`Establishing Figma baseline...`);
    const figmaReferenceConfig = {
        apiKey: originalConfig.apiKey,
        // Same server + proxy as the real test — otherwise on-prem users get the baseline on the public cloud.
        eyesServerUrl: originalConfig.eyesServerUrl,
        proxy: originalConfig.proxy,
        batch: originalConfig.batch,
        appName: originalConfig.appName,
        testName: originalConfig.testName,
        environment: { hostingApp: 'Figma', os: '', viewportSize: figmaViewport },
        properties: (0, utils_1.figmaMetadataToProperties)(realResults[0].metadata, mode),
        baselineEnvName: figmaEnvName,
        saveNewTests: true,
        saveDiffs: true,
        keepBatchOpen: true,
    };
    if (originalConfig.branchName !== undefined)
        figmaReferenceConfig.branchName = originalConfig.branchName;
    const checks = results.map(({ imagePath }, i) => ({
        imagePath,
        tag: figmaUrls[i] === '' ? target_config_1.PLACEHOLDER_TAG : (0, utils_1.figmaStepTag)(figmaUrls[i]),
    }));
    const { isNew: figmaIsNew, diffs: figmaDiffs } = await deps.runFigmaCheck(figmaReferenceConfig, checks);
    logger.log(`Figma baseline: ${figmaIsNew ? 'new (first run)' : !figmaDiffs ? 'passed (design unchanged)' : 'changed (design updated)'}`);
    logger.log(`Linking test to Figma baseline: ${figmaEnvName}`);
    return (0, target_config_1.buildTargetTestConfig)(originalConfig, realResults[0].metadata, figmaViewport, { figmaEnvName, useLatestEnvBaseline: mode === 'auto-baseline' }, mode);
}
exports.setFigmaBaselineFlow = setFigmaBaselineFlow;
async function setFigmaBaseline(configuration, figmaBaselines, opts) {
    var _a;
    const logger = (_a = opts === null || opts === void 0 ? void 0 : opts.logger) !== null && _a !== void 0 ? _a : (0, logger_1.makeLogger)();
    const mode = (0, utils_1.resolveFigmaMode)(opts === null || opts === void 0 ? void 0 : opts.mode);
    if (mode === 'disabled') {
        logger.log('setFigmaBaseline: disabled (APPLITOOLS_FIGMA_MODE=disabled) — skipping.');
        return;
    }
    const getToken = () => {
        var _a;
        if (!(opts === null || opts === void 0 ? void 0 : opts.skipApi) && !(opts === null || opts === void 0 ? void 0 : opts.accessToken)) {
            throw new Error('setFigmaBaseline: no Figma access token. ' +
                'Pass accessToken or set the FIGMA_ACCESS_TOKEN environment variable.');
        }
        return (_a = opts === null || opts === void 0 ? void 0 : opts.accessToken) !== null && _a !== void 0 ? _a : '';
    };
    const restOpts = {
        tempPath: opts === null || opts === void 0 ? void 0 : opts.tempPath,
        nodesCacheTtlSeconds: opts === null || opts === void 0 ? void 0 : opts.nodesCacheTtlSeconds,
        maxRateLimitWaitSeconds: opts === null || opts === void 0 ? void 0 : opts.maxRateLimitWaitSeconds,
        skipApi: opts === null || opts === void 0 ? void 0 : opts.skipApi,
        proxy: opts === null || opts === void 0 ? void 0 : opts.proxy,
        req: opts === null || opts === void 0 ? void 0 : opts.req,
        logger,
    };
    const deps = {
        fetchFigmaImages: urlList => (0, client_1.fetchFigmaImages)(urlList, getToken(), restOpts),
        fetchFigmaMetadata: urlList => (0, client_1.fetchFigmaMetadata)(urlList, getToken(), restOpts),
        createPlaceholderImage: (width, height) => { var _a; return (0, figma_images_1.createPlaceholderPng)(width, height, (_a = opts === null || opts === void 0 ? void 0 : opts.tempPath) !== null && _a !== void 0 ? _a : os.tmpdir()); },
        padDesignImage: (imagePath, width, height, minViewportSize) => { var _a; return (0, figma_images_1.createPaddedDesignImage)(imagePath, width, height, minViewportSize, (_a = opts === null || opts === void 0 ? void 0 : opts.tempPath) !== null && _a !== void 0 ? _a : os.tmpdir()); },
        runFigmaCheck: run_figma_check_1.runFigmaCheck,
    };
    const figmaUrls = (0, utils_1.resolveFigmaUrls)(figmaBaselines, configuration.testName);
    if (figmaUrls === null)
        return;
    const updated = await setFigmaBaselineFlow(figmaUrls, configuration, deps, mode, {
        maxViewportHeight: opts === null || opts === void 0 ? void 0 : opts.maxViewportHeight,
        minViewportSize: opts === null || opts === void 0 ? void 0 : opts.minViewportSize,
    }, logger);
    Object.assign(configuration, updated);
}
exports.setFigmaBaseline = setFigmaBaseline;
