"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildTargetTestConfig = exports.deduplicateBrowsers = exports.PLACEHOLDER_TAG = exports.DEFAULT_MAX_VIEWPORT_HEIGHT = void 0;
const utils_1 = require("./utils");
// Default height cap (overridable via opts.maxViewportHeight): designs can be arbitrarily tall, so
// height is capped but width never is — width is the form factor the design and real test must share.
exports.DEFAULT_MAX_VIEWPORT_HEIGHT = 800;
// Dashboard tag for a '' (placeholder) step; real steps use the Figma URL as the tag.
exports.PLACEHOLDER_TAG = '(no design)';
// UFG opens request per-browser environments; classic sizes a single viewport instead.
function deduplicateBrowsers(environments, targetWidth, targetHeight) {
    const desktop = new Map();
    for (const environment of environments) {
        const db = environment;
        const entry = typeof db.width === 'number'
            ? db
            : { name: 'iosDeviceInfo' in environment ? 'safari' : 'chrome', width: targetWidth, height: targetHeight };
        desktop.set(String(entry.name), { ...entry, width: targetWidth, height: targetHeight });
    }
    return [...desktop.values()];
}
exports.deduplicateBrowsers = deduplicateBrowsers;
// baselineLink is null in "test-baseline" mode: baselineEnvName is left untouched (no change detection
// happened to justify linking), but viewport/properties are still adjusted. figmaViewport always wins.
function buildTargetTestConfig(originalConfig, metadata, figmaViewport, baselineLink, mode) {
    var _a, _b, _c;
    const isUFG = ((_b = (_a = originalConfig.environments) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0) > 0;
    const mergedProperties = [...((_c = originalConfig.properties) !== null && _c !== void 0 ? _c : []), ...(0, utils_1.figmaMetadataToProperties)(metadata, mode)];
    // baselineEnvOptions is the internal field backing Configuration.setBaselineEnvName(name, {useLatestEnvBaseline}) — same effect without a live Configuration.
    const baselineFields = baselineLink
        ? {
            baselineEnvName: baselineLink.figmaEnvName,
            baselineEnvOptions: { useLatestEnvBaseline: baselineLink.useLatestEnvBaseline },
        }
        : {};
    // The figma viewport always wins; merged into environment.viewportSize so os/hostingApp survive.
    const environment = { ...originalConfig.environment, viewportSize: figmaViewport };
    if (isUFG) {
        return {
            ...baselineFields,
            properties: mergedProperties,
            environments: deduplicateBrowsers(originalConfig.environments, figmaViewport.width, figmaViewport.height),
            environment,
        };
    }
    return {
        ...baselineFields,
        properties: mergedProperties,
        environment,
    };
}
exports.buildTargetTestConfig = buildTargetTestConfig;
