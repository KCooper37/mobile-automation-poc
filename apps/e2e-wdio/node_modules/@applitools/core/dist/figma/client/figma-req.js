"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeFigmaReq = exports.FIGMA_API = void 0;
// The Figma REST transport: `@applitools/req` configured for Figma's rate limits.
const req_1 = require("@applitools/req");
exports.FIGMA_API = 'https://api.figma.com/v1';
// `limit` is what bounds the number of retries, not `retryTimeout`: Figma may answer `Retry-After: 0`, and
// a zero delay never advances an elapsed-time budget.
const RETRY_LIMIT = 5;
// Only used when Figma omits `Retry-After`.
const RETRY_BACKOFF_MS = [1000, 2000, 5000, 10000];
function makeFigmaReq({ proxy, logger, maxRateLimitWaitSeconds = 60 } = {}) {
    const maxWaitMs = maxRateLimitWaitSeconds * 1000;
    return (0, req_1.makeReq)({
        proxy,
        retry: {
            limit: RETRY_LIMIT,
            timeout: RETRY_BACKOFF_MS,
            /**
             * Deciding here rather than with `statuses: [429]` is what enforces the wait budget. `req` sleeps
             * `Retry-After` in full and only compares `retryTimeout` against time already ELAPSED, so accepting a
             * retry commits to the whole delay: an hour, a day or a week each block for their full duration.
             * (Beyond ~24.8 days it overflows `setTimeout` and fires almost immediately — wrong in the other
             * direction.) Refusing the retry surfaces the 429 to the caller instead.
             */
            validate: ({ response }) => {
                if ((response === null || response === void 0 ? void 0 : response.status) !== 429)
                    return false;
                const retryAfterMs = Number(response.headers.get('Retry-After')) * 1000;
                // Unparseable (e.g. an HTTP-date) reads as NaN: retry on the backoff schedule rather than trust it.
                if (!Number.isFinite(retryAfterMs))
                    return true;
                if (retryAfterMs > maxWaitMs) {
                    logger === null || logger === void 0 ? void 0 : logger.log(`Figma rate limit — Retry-After of ${retryAfterMs / 1000}s exceeds the ${maxRateLimitWaitSeconds}s budget; giving up.`);
                    return false;
                }
                // Logged here because `beforeRetry` fires only after the sleep, leaving a long wait unexplained.
                if (retryAfterMs > 0)
                    logger === null || logger === void 0 ? void 0 : logger.log(`Figma rate limit — waiting ${retryAfterMs / 1000}s before retry...`);
                return true;
            },
        },
        retryTimeout: maxWaitMs,
    });
}
exports.makeFigmaReq = makeFigmaReq;
