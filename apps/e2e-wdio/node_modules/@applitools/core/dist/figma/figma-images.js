"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPaddedDesignImage = exports.createPlaceholderPng = void 0;
// Image helpers for the Figma resolver: the white placeholder PNG for '' steps and the minViewportSize
// padding that centers a small design on a larger canvas. Both keyed by output dimensions so repeats reuse the file.
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const image_1 = require("@applitools/image");
const utils_1 = require("./utils");
// Writes (or reuses) a placeholder PNG keyed by dimensions alone — content is always identical.
async function createPlaceholderPng(width, height, tempPath) {
    const imagePath = path.join(tempPath, `figma-placeholder-${width}x${height}.png`);
    if (!fs.existsSync(imagePath)) {
        fs.writeFileSync(imagePath, await (0, image_1.generateWhitePng)(width, height));
    }
    return imagePath;
}
exports.createPlaceholderPng = createPlaceholderPng;
// Pads imagePath onto a centered white canvas at paddedDesignSize, returning the new path + canvas size
// (unchanged if it already meets minSize on both axes). Keyed by basename + canvas size so repeats reuse the file.
async function createPaddedDesignImage(imagePath, width, height, minSize, tempPath) {
    const canvas = (0, utils_1.paddedDesignSize)(width, height, minSize);
    if (canvas.width === width && canvas.height === height) {
        return { imagePath, width, height };
    }
    const baseName = path.basename(imagePath, path.extname(imagePath));
    const outputPath = path.join(tempPath, `figma-padded-${baseName}-${canvas.width}x${canvas.height}.png`);
    if (!fs.existsSync(outputPath)) {
        const canvasImage = (0, image_1.makeImage)(await (0, image_1.generateWhitePng)(canvas.width, canvas.height));
        canvasImage.copy((0, image_1.makeImage)(imagePath), {
            x: Math.floor((canvas.width - width) / 2),
            y: Math.floor((canvas.height - height) / 2),
        });
        // toPng (not toFile) — Image.toFile writes the pre-transform pixels, dropping the copy above.
        fs.writeFileSync(outputPath, await canvasImage.toPng());
    }
    return { imagePath: outputPath, ...canvas };
}
exports.createPaddedDesignImage = createPaddedDesignImage;
