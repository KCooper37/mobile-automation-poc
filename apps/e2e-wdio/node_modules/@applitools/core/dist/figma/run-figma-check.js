"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.runFigmaCheck = void 0;
const utils = __importStar(require("@applitools/utils"));
const core_1 = require("../core");
// Cached because this runs once per open — eyes-storybook opens per story, and a fresh Core each time
// re-initialises and forfeits HTTP agent reuse. Safe to share: all state arrives via `openEyes`.
const getFigmaCore = utils.general.cachify(() => (0, core_1.makeCore)({ agentId: 'core.figma-baseline' }));
/**
 * Establishes/updates the Figma-reference baseline via a throwaway headless classic image test (checks
 * each PNG, closes, deletes the result — the saved baseline persists). Uses core's own API, not the
 * eyes layer (core cannot depend on `@applitools/eyes`).
 */
async function runFigmaCheck(config, checks) {
    var _a;
    const core = getFigmaCore();
    const eyes = await core.openEyes({
        type: 'classic',
        settings: {
            appName: config.appName,
            testName: config.testName,
            apiKey: config.apiKey,
            eyesServerUrl: config.eyesServerUrl,
            proxy: config.proxy,
            baselineEnvName: config.baselineEnvName,
            properties: config.properties,
            batch: config.batch,
            keepBatchOpen: config.keepBatchOpen,
            saveDiffs: config.saveDiffs,
            ...(config.branchName !== undefined ? { branchName: config.branchName } : {}),
            environment: config.environment,
            // Throwaway reference test keyed purely by baselineEnvName; skip the (slow) git extraction.
            ignoreGitBranching: true,
        },
    });
    for (const { imagePath, tag } of checks) {
        await eyes.check({ target: { image: imagePath }, settings: { name: tag } });
    }
    // core's close-time equivalent of saveNewTests (defaulted true there too).
    await eyes.close({ settings: { updateBaselineIfNew: (_a = config.saveNewTests) !== null && _a !== void 0 ? _a : true } });
    const [result] = await eyes.getResults({ settings: { throwErr: false } });
    try {
        await core.deleteTest({
            settings: {
                testId: result.id,
                batchId: result.batchId,
                secretToken: result.secretToken,
                eyesServerUrl: result.eyesServer.eyesServerUrl,
                apiKey: result.eyesServer.apiKey,
                proxy: result.eyesServer.proxy,
            },
        });
    }
    catch {
        // best-effort cleanup
    }
    return { isNew: result.isNew, diffs: result.mismatches > 0 };
}
exports.runFigmaCheck = runFigmaCheck;
