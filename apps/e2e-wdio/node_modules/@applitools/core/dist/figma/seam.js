"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyFigmaBaselines = void 0;
const utils = __importStar(require("@applitools/utils"));
const figma_1 = require("./figma");
const utils_1 = require("./utils");
/**
 * @internal
 * Resolves the `figmaBaselines` / `figmaOptions` open config once per open, then always strips both — they
 * are off-the-wire and must never be logged or sent. The resolver mutates `settings` in place.
 *
 * The only place the Figma feature reads the environment; everything below it takes parameters. Each
 * `figmaOptions` field wins over its env fallback.
 */
async function applyFigmaBaselines(settings, logger, resolve = figma_1.setFigmaBaseline) {
    var _a, _b, _c, _d, _e, _f;
    const figmaBaselines = settings.figmaBaselines;
    const figmaOptions = (_a = settings.figmaOptions) !== null && _a !== void 0 ? _a : {};
    if (!figmaBaselines) {
        // Undefined-valued keys must not survive to the wire/log.
        delete settings.figmaBaselines;
        delete settings.figmaOptions;
        return;
    }
    try {
        // A shared open config applies to every test, so an unmapped test must not fail — only skip.
        if (typeof figmaBaselines !== 'string' && figmaBaselines[settings.testName] === undefined) {
            logger.warn(`figmaBaselines is set but has no entry for test "${settings.testName}"; skipping Figma baseline resolution for this test.`);
            return;
        }
        // skipApi has no figmaOptions equivalent by design — CI/debug switch, not a user option.
        const skipApi = (_b = utils.general.getEnvValue('FIGMA_SKIP_API', 'boolean')) !== null && _b !== void 0 ? _b : false;
        const envTtl = utils.general.getEnvValue('FIGMA_NODES_CACHE_TTL', 'number');
        const tempPath = (_c = figmaOptions.tempPath) !== null && _c !== void 0 ? _c : utils.general.getEnvValue('FIGMA_TEMP_PATH');
        // A long TTL lets a pre-warmed cache (committed fixtures) serve through the normal cache-hit path
        // instead of the all-or-nothing skipApi bypass. Number.isFinite also rejects a non-numeric env value.
        const nodesCacheTtlSeconds = (_d = figmaOptions.nodesCacheTtlSeconds) !== null && _d !== void 0 ? _d : envTtl;
        // Registered with the logger the way apiKey/secretToken are elsewhere, so a token supplied through
        // figmaOptions is redacted wherever settings are logged downstream.
        const accessToken = (_e = figmaOptions.accessToken) !== null && _e !== void 0 ? _e : process.env.FIGMA_ACCESS_TOKEN;
        if (accessToken)
            logger.mask(accessToken);
        await resolve(settings, figmaBaselines, {
            logger,
            skipApi,
            mode: (0, utils_1.resolveFigmaMode)((_f = figmaOptions.mode) !== null && _f !== void 0 ? _f : utils.general.getEnvValue('FIGMA_MODE')),
            accessToken,
            // On-prem accounts have no direct route to api.figma.com.
            proxy: settings.proxy,
            ...(tempPath ? { tempPath } : {}),
            ...(Number.isFinite(nodesCacheTtlSeconds) ? { nodesCacheTtlSeconds } : {}),
            // These three have no env fallback, so omit them when absent and let the resolver default them.
            // `!= null` not `!== undefined`: bindings that serialize an unset nested field as null (Ruby) would
            // otherwise reach `maxRateLimitWaitSeconds = 60`, which only defaults on undefined, and null*1000
            // silently zeroes the rate-limit budget.
            ...(figmaOptions.maxRateLimitWaitSeconds != null
                ? { maxRateLimitWaitSeconds: figmaOptions.maxRateLimitWaitSeconds }
                : {}),
            ...(figmaOptions.maxViewportHeight != null ? { maxViewportHeight: figmaOptions.maxViewportHeight } : {}),
            ...(figmaOptions.minViewportSize != null ? { minViewportSize: figmaOptions.minViewportSize } : {}),
        });
    }
    finally {
        delete settings.figmaBaselines;
        delete settings.figmaOptions;
    }
}
exports.applyFigmaBaselines = applyFigmaBaselines;
