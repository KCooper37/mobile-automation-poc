import type { Account } from './types';
/** @internal Decide once per process whether to stream the log buffer to the
 *  Azure blob. Called from `getAccountInfo`. */
export declare function decideSdkLogStreaming(account: Account): void;
